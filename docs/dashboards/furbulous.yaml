# Furbulous litter-box dashboard — THIS HOUSE (<HA_HOST>)
#
# Prefer the live dashboard (already updated via API):
#   http://<HA_HOST>:8123/dashboard-furbulous/boxes
#
# Full Lovelace JSON (restore/raw paste source of truth):
#   docs/dashboards/furbulous.lovelace.json
#
# Entity prefixes on this HA (do NOT use short downstairs_/master_ alone):
#   Downstairs → family_room_downstairs_
#   Master     → master_bedroom_master_
#   Cleo       → cleo_  (+ middle_bedroom_cleo_ for toilet/bag_status/last_cleaned/no_bag/needs_cleaning)
#   Upstairs   → front_bedroom_upstairs_
#
# Root cause of mass "Unknown": dashboard referenced sensor.downstairs_* etc.,
# which do not exist here. Area-qualified IDs above are required.
#
# Pause hub entities (switch.furbulous_pause_cloud_polling, …) need Furbulous
# 1.3.16+. If missing, update via HACS and restart HA.
#
# To re-import JSON: install "lovelace-gen" is not required — use
# Dashboards → Furbulous → ⋮ → Raw configuration editor is YAML-only.
# Easiest restore: keep using the live storage dashboard (already fixed).
