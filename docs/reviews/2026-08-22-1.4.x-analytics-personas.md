# 1.4.x analytics + dashboard — persona review

**Scope:** 1.4.0 engine (A1–A3, B1–B2), 1.4.1 dashboard Visits row, 1.4.2 gold test alignment.

## Principal developer

- Clean finish without Dirty (A1) closes a real product hole; flags still clear so idle polls do not re-fire.
- Edged-property timestamps + skew (A2) match the 1.3.22 intent without cold-start seeding sticky times.
- Cloud pack → bag_replaced (B1) matches Seal=new bag; debounce shared with HA button.
- WC↔presence dedup (B2) is start/leave-window based; presence-after-WC still possible — acceptable for P2.

## Matei Zaharia (systems)

- Prefer small, testable edges over rewriting the store.
- Local EventStore remains source for 7d/30d; cloud WC is today-only — correct split.
- Do not drive Dirty from WC (false red after restart).

## Security

- No new secrets; dashboard YAML still forbids LAN IPs/tokens.
- Property times only applied after local edges + sanity bounds.

## HA expert / quality

- Bronze/silver/gold: **50 passed** after gold cover-bit fix (128=No Bag, 512=lid).
- Dashboard: status → chips → Visits → actions; unknown segments omitted; zeros kept.
- UAT: lovelace `dashboard-furbulous` saved with Visits cards; integration rsynced + container restarted.

## UI expert

- One compact “Visits” mushroom card per box (not a wall of unknowns).
- Status secondary rebuilt with `namespace` so Litter/Bag/Cleaned only appear when known.
- Consistency: same card on all four boxes even when some metrics are `0`.

## Follow-ups (later 1.4.x)

- Browser pass on mobile viewport once browser tools available.
- Optional: presence-after-WC dedup; measure double-count in prod store.
